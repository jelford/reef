"""
:synopsis: Holds all the handlers to be used by the reef server.

This is where any handlers should be placed.
Ideally code not related to handling server requests should be elsewhere.

To see where (or if) these handlers are attached to the server, see the
:mod:`handlers` module.
"""
